/**
 * 非WLAN网络流量枚举
 */
export enum DownloadWithoutWlanEnum {
  downloadBigPic = 0,
  downloadSmart,
  downloadDisable,
}

/**
 * 非WLAN网络播放提醒
 */
export enum RemindWithoutWlanEnum {
  once = 0,
  always,
}