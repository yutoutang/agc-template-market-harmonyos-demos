/**
 * 编辑器控制器
 */
export class EasyEditorController {
  /**
   * 设置编辑器内容
   * @param content 要设置的内容
   */
  setContent(content: string) {
  }

  /**
   * 获取编辑器内容
   * @returns 获取到的内容
   */
  getContent(): string {
    return ''
  }
}