export interface ProductSkuItem {
  productId: string;
  skuCode: string;
  skuDesc: string;
  stock: number;
  banner: string;
  title: string;
  serviceDesc: string;
  price: number;
  dashPrice?: number;
}
